#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a = 10, b =20;
    if (a>b); // semi colon is always wrong also if is terminated with a semicolon, else cannot be there without if.


    {
        printf("Hello World!");
    }

    printf("This line will always be printed\n");
    return 0;
}
